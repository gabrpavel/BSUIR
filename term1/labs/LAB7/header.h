#include <stdio.h>
#include <time.h>
#include <stdlib.h>


void reccurence(int* reset);
void choose_task(int* choice);

int str_len(char** str);
void print(char** str);

void getStr(char** string, int length);
int if_letter(char s);

int input_N(int N);


int find_inputed_word(char** str, int N);
void reverse_str(char** str, int K, int M);
int end_of_word(char** str, int posM);
void reverse_word(char** str, int begin);

void reverse_word(char** str, int begin);

int count_of_occurences(char** s, char** s0);



task1();

task2();
